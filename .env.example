# C2C Configuration
# Make a copy of this file named '.env' and fill in your secure values.
# DO NOT commit '.env' to version control!

# A long, secure random string (minimum 32 characters)
SECRET_KEY=replace-with-a-secure-random-value

# PostgreSQL Connection String (e.g., for local development or production)
# Format: postgresql://username:password@hostname:port/dbname
DATABASE_URL=postgresql://user:pass@localhost:5432/c2cdb

# Set to '1' in production if serving over HTTPS to ensure secure cookies
COOKIE_SECURE=1
